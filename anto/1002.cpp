#include <iostream>
#include <fstream>
using namespace std;


int n,lm1,lm2;
char s[]="el mergea tot spre aleea pierduta";

void adaugare(char s[ ],char x[ ]){
    int i;
    int ls=strlen(s), lx=strlen(x);
    for(i=1; i<=lx; i++){
        s[ls]
    }


}

int main(){

    int n=5,lungime;

    char *p=strtok(s," ");

    while(p){
        lungime=strlen(p);
        if(lungime>=n)
            adaugare(m1,p);
        else adaugare(m2,p);

        p=strtok(NULL," ");
    }




return 0;
}

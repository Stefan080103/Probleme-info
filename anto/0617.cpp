#include <iostream>
using namespace std;
int main()
{
    int l,pi,pf,nr=0;
    char a[256];
    cin.getline(a,256);
    l=strlen(a);
    for(i=0;i<l;i++){
        if(a[i]<a[i+1]){
            nr++;
            pi=
            Max=nr;
        }else nr=0;
    }

    return 0;
}
